name: Deploy to Server

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3  # Aktualizacja do najnowszej wersji

    - name: Setup SSH
      uses: webfactory/ssh-agent@v0.8.0  # Aktualizacja do wersji kompatybilnej z Node.js 20
      with:
        ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

    - name: Deploy to server
      env:
        SSH_HOST: ${{ secrets.SSH_HOST }}
        SSH_USERNAME: ${{ secrets.SSH_USERNAME }}
      run: |
        mkdir -p ~/.ssh
        echo "$SSH_PRIVATE_KEY" > ~/.ssh/id_rsa
        chmod 600 ~/.ssh/id_rsa
        ssh-keyscan -H $SSH_HOST >> ~/.ssh/known_hosts
        ssh $SSH_USERNAME@$SSH_HOST 'cd ~/websites/blatypodwymiar.pl && git pull origin main && npm install && npm run build'
